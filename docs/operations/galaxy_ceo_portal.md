# 银河 CEO 门户数据出口交付与运行留痕

本文记录当前已完成的工作和本机运行状态。数据库凭据、飞书 App 密钥、电子表格 token 不写入本文档；它们只通过受控 `.env` 配置入口使用。

## 2026-09-14 交付记录

### 已完成事项

1. 保留原有节目包维度 pipeline：按公司、业务类型、节目包和日期写入 `银河CEO门户原始数据`。
2. 新增独立的大区维度 pipeline：按公司总计行和大区行写入 `银河CEO门户大区原始数据`；代码、配置项、锁文件、service 和 timer 均与节目包 pipeline 分开。
3. 大区表的窗口策略调整为：最近 400 个自然日，即数据库共同最新日期与北京时间昨日两者较早者，向前回溯 399 天。
4. 两个 pipeline 都保留数据库明确提供的总计行，不把大区或城市明细相加替代总计。
5. 大区表的公司/大区对应表和汇率税率表作为参考 Sheet 保留；日常原始数据更新器不覆盖这两个参考 Sheet。
6. 两个日更入口均向 `BI Plus Reporting` 发送成功/失败卡片；卡片包含产出表格链接、表格状态、执行动作和轻量质检摘要。
7. 日更质检至少覆盖数据库时效、表头/空行/字段类型、日期窗口完整性、完整业务键不重复、来源指标冲突以及写入后回读；大区任务额外核对公司总计与大区行。

### 大区表初始验收

- 初始业务日期范围：`2026-08-01` 至 `2026-09-13`。
- 原始数据行数：15,268 行；其中公司总计行 2,200 行、大区行 13,068 行。
- 数据库公司总计与大区明细做了逐组校验：人数指标无硬差异；金额差异仅为分币级舍入差异，未用求和结果替换数据库原始行。
- 已从原节目包表和新大区表抽取乌干达、尼日利亚两个公司的一天数据核对：公司总计和人数指标一致，金额最多出现 0.01 USD 的展示舍入差异。
- 旧版日更 dry-run 已验证：数据库共同最新日和大区表最新日均为 `2026-09-13`；当时目标窗口为 `2026-08-01` 至 `2026-09-13`，读取最新日 347 行，待追加 0 行，待删除 0 行。窗口改为 400 天后，下一次运行会自动回补 `2025-08-10` 起缺少的历史数据。

### 定时任务状态

本机用户级 systemd 已执行 daemon-reload，并已启用、启动以下两个 timer：

| Timer | 北京时间 | 重试策略 | Service |
| --- | ---: | --- | --- |
| `galaxy-ceo-portal-region-update.timer` | 每日 15:55 | 16:55、17:55、18:55，最多额外 3 次 | `galaxy-ceo-portal-region-update.service` |
| `galaxy-ceo-portal-update.timer` | 每日 16:00 | 17:00、18:00、19:00，最多额外 3 次 | `galaxy-ceo-portal-update.service` |

启用后通过 `systemctl --user list-timers --all` 确认两个 timer 均已登记；本次检查显示下一次触发为 `2026-09-15 15:55` 和 `2026-09-15 16:00`（北京时间）。这个状态属于当前机器的运行状态，迁移机器时必须重新安装并检查。

### 代码与验证

- 大区初始装载：`src/galaxy_ceo_portal_region_data.py`。
- 大区每日更新：`src/galaxy_ceo_portal_region_daily_update.py`。
- 大区测试：`tests/test_region_daily_update.py`。
- 大区部署模板：`deploy/systemd/galaxy-ceo-portal-region-update.service`、`deploy/systemd/galaxy-ceo-portal-region-update.timer`。
- 最近一次验证：全量测试 25 项通过，两个 Python 入口编译通过；通知发送为尽力而为，失败只记日志，不阻断数据任务。

## 后续维护约束

- 任何调整前先区分节目包 pipeline 与大区 pipeline，不共用表格 token，也不要把一个 pipeline 的窗口策略复制到另一个。
- 修改指标、SQL 筛选条件或字段含义时，先更新对应产品文档和测试，再执行 dry-run；禁止用城市/大区求和掩盖源数据总计差异。
- 参考数据（公司、大区、汇率、税率）发生变化时，单独记录来源、有效期和更新时间，不把它混入原始数据日更任务。
- Reporting 配置使用 `FEISHU_REPORTING_CHAT_NAME`（默认 `BI Plus Reporting`）和 `FEISHU_SPREADSHEET_BASE_URL`；两者均为非密钥配置，App 密钥仍只从受控 env 文件读取。

## 2026-09-15 运行修复与成功回读

- 首次运行因 `metric_sub_recharge_money` 的 0/非 0 占位冲突而 fail-closed；经节目包明细核对后，仅保留与明细合计一致的非 0 值。
- 发现 3 个新增用户 v2 公司总计为 0 的日期；旧版公司总计与大区明细完全一致后，按记录的回退规则修复。
- 实际更新成功：窗口 `2025-08-11` 至 `2026-09-14`，回读 137,506 行，400 天完整覆盖，重复业务键 0 个。
- 产出表：`银河CEO门户大区原始数据 2026-09-15 17:26`；通知已发送到 `BI Plus Reporting`。
- 质量卡为警告状态，仅包含 8,585 个金额分币舍入差异；失败质检项为 0。
